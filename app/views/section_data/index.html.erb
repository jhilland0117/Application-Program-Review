<h1>Listing section_data</h1>

<table>
  <thead>
    <tr>
      <th>Data</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @section_data.each do |section_datum| %>
      <tr>
        <td><%= section_datum.data %></td>
        <td><%= link_to 'Show', section_datum %></td>
        <td><%= link_to 'Edit', edit_section_datum_path(section_datum) %></td>
        <td><%= link_to 'Destroy', section_datum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Section datum', new_section_datum_path %>
