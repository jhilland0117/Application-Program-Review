<%= form_for(@apr_report) do |f| %>
  <% if @apr_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apr_report.errors.count, "error") %> prohibited this apr_report from being saved:</h2>

      <ul>
      <% @apr_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
